# Local dependencies and build outputs should not be sent to Docker build context.
node_modules
.pnpm-store
.next
out
coverage
playwright-report
test-results

# Local secrets and environment overrides must stay outside images.
.env
.env.local
.env.*.local

# Repository and OS noise.
.git
.DS_Store
Thumbs.db
