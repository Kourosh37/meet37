// Meeting route group layout placeholder.
//
// Planned responsibilities:
// - Provide shared meeting shell styles.
// - Keep public room/create/prejoin routes independent from admin navigation.
// - Reserve future hooks for device permission state and meeting teardown.

import type { ReactNode } from "react";

export default function MeetLayout({ children }: { children: ReactNode }) {
  return children;
}
