# Frontend Skeleton

This folder is the planned Next.js frontend for the meeting product.

Current state:

- The project structure follows `ai_doc.txt`.
- The project structure follows `frontend-architecture.md`.
- Files are intentionally comment-only or minimal configuration placeholders.
- Business logic, UI implementation, API clients, WebSocket signaling, WebRTC, and tests are not implemented yet.

Next implementation phase:

- Install dependencies with `pnpm install`.
- Initialize Next.js, Tailwind, shadcn/ui, Zustand, React Query, and Playwright.
- Replace placeholder comments with actual implementation module by module.
