# Integration Test Skeleton

Planned coverage:

- Feature components with mocked REST/WebSocket dependencies.
- Auth refresh behavior across API client and auth store.
- Meeting state transitions across signaling messages.
- Admin settings and user mutation flows.
